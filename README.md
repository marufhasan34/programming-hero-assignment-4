1. we used getElementById to get the id from the element.and we used getElementsByClassName to get the class from the element.and most importantly they returned as an HTML collection.on the other hand we used querySelector for single element.and we also used  querySelectorAll which is for multiple elements.querySelector and querySelectorAll returns as an node list.

2.we used document.createElement() to create and insert a new element into the DOM.

3.when an event triggered on a child element automatically propagates up to its parent elements in the DOM that is Event Bubbling.

4.Event Delegation is using a single parent listener to handle events for its child elements, leveraging event bubbling for efficiency and dynamic elements.

5.preventDefault() is for stop the browser and stopPropagation() is for stop the event.